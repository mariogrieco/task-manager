{
  "name": "task-manager-consumer",
  "version": "1.0.0",
  "main": "./index.ts",
  "types": "./src/types.tsc",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "prepublish": "npm run build"
  },
  "files": ["dist"],
  "devDependencies": {
    "@vitest/coverage-v8": "^1.5.0",
    "typescript": "^5.3.3",
    "vitest": "^1.5.0",
    "zod": "^3.22.4"
  },
  "dependencies": {
    "axios": "^1.6.2"
  }
}
